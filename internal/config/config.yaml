app:
  name: whatsapi
  port: 8080
database:
  host: localhost
  user: admin
  password: secret
cache:
  file: file:internal/cache/whatsapicache.db?_foreign_keys=on
  storage: sqlite3
appenv: 
  env: development
  logfile: whatsapi.logs
